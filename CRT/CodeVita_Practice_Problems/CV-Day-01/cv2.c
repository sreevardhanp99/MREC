#include<stdio.h>
void main()
{
	char s[10],op[10];
	int idx,r=0,sum;
	scanf("%s",s);
	for(int i=0;s[i]!='\0';i++)
	{
		if(s[i]=="a" || s[i]=="e" || s[i]=="i" || s[i]=="o" || s[i]=="u" )
		{
			idx=i;
			sum=0;
			for(j=1;j<idx*5;j++)
			{
					
			}
		}
		else
		{
			op[r++]=s[i];
		}
	}

}
